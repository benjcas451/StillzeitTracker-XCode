name: iOS IPA (Release)

on:
  workflow_dispatch:

jobs:
  build-ipa:
    name: Unsignierte IPA bauen und als Release veröffentlichen
    runs-on: macos-latest
    # GITHUB_TOKEN braucht Schreibrechte, um Tag + Release anzulegen.
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      # Unsigniert bauen (Sideloading signiert beim Installieren selbst).
      # Die Watch-App wird als Abhängigkeit mitgebaut und ins App-Bundle
      # eingebettet (stillzeit.app/Watch/StillzeitWatch.app).
      # CURRENT_PROJECT_VERSION = run_number haelt CFBundleVersion strikt
      # steigend, damit Update-Erkennung beim Sideloading funktioniert.
      - name: App bauen (Release, iphoneos, ohne Signierung)
        run: |
          xcodebuild \
            -project stillzeit.xcodeproj \
            -scheme stillzeit \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGN_IDENTITY="" \
            CURRENT_PROJECT_VERSION=${{ github.run_number }} \
            build

      - name: IPA packen
        run: |
          mkdir Payload
          cp -R build/Build/Products/Release-iphoneos/stillzeit.app Payload/
          zip -qq -r -9 Stillzeit.ipa Payload

      - name: IPA als GitHub-Release veröffentlichen
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: Stillzeit.ipa
          asset_name: Stillzeit-v2.2.3-${{ github.run_number }}.ipa
          tag: v2.2.3-${{ github.run_number }}
          overwrite: true
          body: "Stillzeit v2.2.3 (Lauf ${{ github.run_number }}) – unsignierte IPA inkl. eingebetteter watchOS-App"
